import 'package:flutter/material.dart';

class AppColors {
  // Primary palette — teal profundo del Stitch
  static const primary = Color(0xFF003B43);
  static const primaryContainer = Color(0xFF1A535C);
  static const onPrimary = Colors.white;

  // Secondary — mint-aqua
  static const secondary = Color(0xFF006A65);
  static const secondaryContainer = Color(0xFF79F3EA);
  static const onSecondary = Colors.white;

  // Accent (del archivo adjunto — salvia)
  static const accentSoft = Color(0xFF9CC5A1);
  static const accentStrong = Color(0xFF3F7D5C);
  static const accentDeep = Color(0xFF2C5A41);
  static const accentTint = Color(0xFFEEF5EF);

  // Backgrounds
  static const background = Color(0xFFF4FAFF);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceLow = Color(0xFFE7F6FF);
  static const surfaceContainer = Color(0xFFE0F0FB);

  // Text
  static const textPrimary = Color(0xFF0E1D25);
  static const textSecondary = Color(0xFF40484A);
  static const textFaint = Color(0xFF9AA39C);

  // Dividers & borders
  static const hair = Color(0xFFE9EDE8);
  static const outline = Color(0xFFC0C8CA);

  // Status
  static const success = Color(0xFF3F7D5C);
  static const successTint = Color(0xFFEEF5EF);
  static const error = Color(0xFFBA1A1A);
  static const errorTint = Color(0xFFFFDAD6);
  static const warning = Color(0xFFB06A00);
  static const warningTint = Color(0xFFFFEDD5);
}
